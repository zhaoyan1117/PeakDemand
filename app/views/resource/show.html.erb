<%= render 'shared/user_header' %>

<%= render :partial => 'top_screen',
           :locals => {
                :resource => @resource,
                :user => @user
           } %>

<%= render :partial => 'bottom_screen',
           :locals => {
                :resource => @resource,
                :cgi_cal_id => @cgi_cal_id,
                :calendar_xml_feed => @calendar_xml_feed,
                :user => @user
           } %>

<%= render :partial => 'show_demand_modal',
		       :locals => { :user => @user } %>

<%= render :partial => 'email_provider_modal',
           :locals => { :resource => @resource } %>